# SmartFeed AI - Production Dependencies
# Core Production Web Framework & Server
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
pydantic>=2.0.0
gunicorn>=21.2.0

# Optional Legacy UI (If Streamlit runner is used)
streamlit>=1.35.0

# Computer Vision & Machine Learning
# Using headless OpenCV for zero-GUI cloud container compatibility
opencv-python-headless>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
Pillow>=10.0.0
plotly>=5.18.0

# QR Digital Feed Passport & Barcode Decoding
qrcode[pil]>=7.4.2
pyzbar>=0.1.9

# Environment & Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Deep Learning (Optional: Runtime inference features adaptive OpenCV fallback)
# torch>=2.0.0
# torchvision>=0.15.0
# tensorflow>=2.15.0

# Optional Cloud LLM Providers (100% offline rule-based advisory operates without these)
# openai>=1.20.0
# anthropic>=0.20.0
